require('./lib/requires');

var s = new Switch({eth1: 'eth1', eth2: 'eth2'});
//s.devices.eth1.addFilter('send', new ChangeVlan({'-': 12}));
//s.devices.eth2.addFilter('send', new ChangeVlan({12: null}));
